%header
  = link_to root_url, :id => :home_link do
    %strong eat
    %span.hckr<> hckr
  -# #account_actions
  -#   - if current_user
  -#     = link_to 'host a meal', new_meal_url, :class => 'strong'
  #navbar{ :style => "padding: 0px 10px 5px 0px; float: right;" }
